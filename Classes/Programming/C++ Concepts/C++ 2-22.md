#school #CS-1300 
# Stream Errors
- Sometimes something causes the stream to enter an error state
	- Example 1: Program tries to read from a file but there is no data in the file
	- Example 2: User inputs a string instead of an integer
- A stream error occurs when insertion or extraction FAILS


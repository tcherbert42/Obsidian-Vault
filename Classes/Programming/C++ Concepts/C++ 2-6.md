#school #CS-1300 
# Module 4 : Branching

##### Logical Operators
- And =  Relational Expression is true if both expressions are true ( %% )
- Or    =  Relation Expression is true if either expression is trye ( || )
- Not  =  Reverses the value of an expression -true expression becomes false/false becomes true ( ! )

Checking Numeric Ranges
```c++
if (grade >= 0 && grade <= 100)
	cout << "Valid Grade";

if (grade < 0 || grade > 100)
	cout << "Invalid grade";

// CANNOT use mathematical notation
if (0 <= grade <= 100) // Doesn't Work!!
```

#####